<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/branco"
    android:paddingLeft="16dp"
    android:paddingTop="20dp">

    <ImageView
        android:id="@+id/iv_car_campanha"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentLeft="true"
        android:layout_marginRight="6dp"
        android:src="@null" />

    <TextView
        android:id="@+id/tv_model_campanha"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="1dp"
        android:layout_toRightOf="@+id/iv_car_campanha"
        android:paddingRight="16dp"
        android:textColor="@color/colorPrimarytext"
        android:textSize="16sp" />

    <View
        android:id="@+id/vw_divider_campanha"
        android:layout_width="match_parent"
        android:layout_height="0.8dp"
        android:layout_alignLeft="@+id/tv_model_campanha"
        android:layout_below="@+id/tv_model_campanha"
        android:layout_marginTop="20dp"
        android:background="@color/colorPrimary" />

</RelativeLayout>
